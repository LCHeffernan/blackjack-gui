import React from "react";

const NewGame = () => (
  <div>
    <button
      className="new-game-button"
      data-testid="new-game-button"
      type="button"
    >
      New Game
    </button>
  </div>
);

export default NewGame;
